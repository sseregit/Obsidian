```text
다음 요구사항을 모두 충족하는 전문가 수준의 테스트 코드를 작성하라.

프로젝트는 Spring Boot 3.4.2 기반이며 테스트 프레임워크는 JUnit 5를 사용한다. 단언은 AssertJ를 활용한다.

모든 테스트 코드는 BDD 스타일(Given–When–Then)로 작성한다.  

정상 시나리오, 엣지 케이스, 경계값 테스트, 예외 처리 시나리오를 모두 포함하며 FIRST 원칙을 준수한다.

테스트 대역은 Mocking보다 Fake 객체 기반으로 구현하고,

DB 관련 테스트는 H2 Embedded Database를 사용하여 실제 쿼리까지 검증하며,

HTTP 레이어 통합 테스트는 MockMvc 기반으로 작성한다.

테스트 메서드 이름은 **한글로, 시나리오를 직관적으로 이해할 수 있도록 명료하게** 작성한다.

또한, **절대로 실패하는 테스트 코드를 제공하지 말고, 모든 테스트가 성공하는 상태까지 완성된 코드만 제공한다.**
  
테스트 코드는 가독성과 유지보수성을 고려해 구조화하며, 실제 서비스의 동작 흐름을 충실히 반영한 현실적 시나리오 중심으로 생성하라.
```
- 기대
	- Mocking보다는 Fake 객체를 사용한 테스트 코드 작성
	- BDD 스타일
	- 정상 시나리오, 엣지 케이스 경계값 테스트, 예외 처리 시나리오를 모두 포함하는 테스트 코드 작성
	- FIRST 원칙 준수

# 수정 1
```markdown
다음 요구 사항을 모두 충족하는 전문가 수준의 테스트 코드를 작성하라.
프로젝트는 Spring Boot 3.4.2 기반이며 테스트 프레임워크는 JUnit 5, 단언은 AssertJ를 사용한다.

정상 시나리오, 엣지 케이스, 경계값 테스트, 예외 처리 시나리오를 모두 포함하고 FIRST 원칙을 준수한다.
테스트 대역은 Mocking보다 Fake 객체 기반으로 구현하는 것을 우선하되,
DIP(의존성 역전 원칙)를 준수할 수 없거나 Fake 구현이 비현실적일 경우 반드시 인터페이스로 추상화를 적용한 뒤 Fake를 구현한다.
절대로 즉흥적이거나 무리한 Fake 객체를 생성하지 말고, 실제 동작을 최대한 현실적으로 반영하는 테스트 대역만 구성하라.

DB 관련 테스트는 H2 Embedded Database를 사용하여 실제 쿼리 레벨까지 검증하며,
HTTP 레이어 통합 테스트는 MockMvc 기반으로 BDD 스타일(Given–When–Then)로 작성한다.

테스트 메서드 이름은 한글로 매우 직관적이며 테스트 의도가 명확히 드러나도록 작성한다.
또한, 실패한 테스트 상태로 결과를 제공하지 말고, 모든 테스트가 정상적으로 성공하는 완성된 코드만 제공한다.

테스트 코드는 가독성·유지보수성을 고려해 구조화하며, 실제 서비스 흐름과 도메인 시나리오를 충실히 반영하여 작성하라.
```
- 테스트 대역은 Mocking보다 Fake 객체 기반으로 구현하고,
	- 이부분은 너무 무분별하고 안맞는 Fake 객체가 생겨날 것이라 생각
		- 상속을 해서 오버라이드로 처리한다던지
		- 너무 복잡해서 그냥 Mocking을 해버린다던지.
	- DIP를 활용하면서 오히려 구조를 바꾸면서 **추상화에 의존하게 변경하는 이득을 얻을 수 있게 된다.**
		- 내가 원하는 Fake 구현체를 작성하는데 쉬워진다.